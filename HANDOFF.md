# 引き継ぎ（MacBook Pro → mac mini）

作成: 2026-08-06 / 引き継ぎ元セッション: MacBook Pro (Mac16,1)

このファイルは、別マシンで作業を再開する Claude Code が最初に読むための引き継ぎ書。
プロジェクトの恒久ルールは `CLAUDE.md`、ここには**いま途中の作業**と**環境差分**を書く。

---

## 1. mac mini 側でのセットアップ

```bash
# 未 clone なら（private リポジトリ。gh auth login 済みであること）
gh repo clone shou12101990-ctrl/masui_ref
# clone 済みなら
git pull --rebase origin main
```

確認すること:

| 項目 | 内容 |
|---|---|
| Flutter | MacBook 側は `/Users/s/Desktop/AI開発環境/flutter/bin/flutter`。mac mini では `which flutter` で確認し、無ければ同等のフルパスを使う。**PATH に入っていない前提でフルパス指定するのが安全** |
| バージョン | CI は Flutter **3.44.1** 固定（`.github/workflows/deploy.yml`）。ローカルも 3.44.x で揃える |
| gh CLI | `gh auth status` で認証確認。デプロイ実行に必須 |
| 依存取得 | 初回は `flutter pub get` |

ビルド確認（デプロイではない）:

```bash
flutter build web --no-tree-shake-icons --base-href /masui_ref/
```

---

## 2. 現在の作業 — 薬剤マスタへの「上限量・投与間隔」追記（**未完 / 最優先**）

### 何をしたいか

薬剤マスタ 248 剤のうち、**上限量・1 日の使用回数の上限・投与間隔の規定がある薬**について、
その規定を専用欄に集約して再記載する。いまは `dose` の文字列の中に散発的に混ざっているだけで一貫していない。

### 済んでいること（コミット前・作業ツリーに変更あり）

`git status` に出る 2 ファイルが該当。

1. **`lib/models/drug.dart`** — `Drug` に `doseLimit` フィールドを追加済み

   ```dart
   /// 上限量・投与間隔の規定. 1回量/1日量の上限, 1日の使用回数の上限,
   /// 再投与までの最短間隔, 総投与期間の上限など「超えてはいけない値」を集約する.
   /// 電子添文に上限の規定がない薬では null.
   final String? doseLimit;
   ```

   コンストラクタの名前付き引数（`this.doseLimit`）と `searchText` への追加も済み。

2. **`lib/screens/drug_detail_screen.dart`** — 詳細画面に表示セクションを追加済み

   「用法・用量」カードの直後に `_HighlightCard(icon: Icons.speed_outlined, title: '上限量・投与間隔', color: Color(0xFFE65100))` を出す。
   `doseLimit` が null または空なら描画しない。

この 2 ファイルは `dart analyze lib` 済み。**追加した変更由来のエラー・警告はゼロ**。

参考までに、リポジトリ全体の analyze はもともと 113 issues（error 1 / warning 10 / 残りは info）が出る。
いずれも今回の変更とは無関係な既存分:

- `services/audio_beep_web.dart:1` の `dart:web_audio` が解決できない（error）— 条件付きインポートの都合で、web ビルド自体は通る。
- `adult_induction_screen.dart` / `gamma_calculator_screen.dart` / `pediatric_screen.dart` の未使用宣言・未使用 import（warning）。
- 残りは `deprecated_member_use`（`value` → `initialValue`）など info。

再開時は「issues が 113 前後のまま増えていないか」で判断すればよい。

### 残っていること

`lib/data/drugs/*.dart`（18 ファイル・計 248 剤）の各 `Drug(...)` に `doseLimit: '...'` を書き込む。
まだ **1 剤も書き込んでいない**。

### 再開のしかた（推奨）

並列ワークフローのスクリプトをリポジトリに置いてある。

```
.claude/workflows/drug-dose-limits.js
```

Claude Code で次のように実行すると、14 グループ × （抽出 → 敵対的検証）の 28 エージェントが Sonnet 5 で走り、
`{groups: [{key, items: [{name, doseLimit}]}], total}` を返す。

```
Workflow({scriptPath: ".claude/workflows/drug-dose-limits.js"})
```

※ MacBook 側で 1 度起動したが、8 エージェント起動時点で中断したため**結果は残っていない**（journal に完了記録なし）。
   mac mini では最初から走らせ直してよい。

返ってきた `{name, doseLimit}` を、対応する `Drug(...)` リテラルの `dose:` の直後に挿入する。
挿入時の注意:

- Dart の文字列リテラルなので `'` と `$` はエスケープが要る（`\'` / `\$`）。改行は `\n`。
- `name:` の値で薬剤を特定する。同名が別ファイルに無いか確認（例: `デキサメタゾン (DEX)` と `デキサメタゾン (制吐)` は別物、`リドカイン` と `リドカイン (抗不整脈)` も別物）。
- `ハプトグロビン (Hp)` はグループ定義で重複している。1 回だけ入れる。

### 記載ルール（ワークフローのプロンプトにも同内容が入っている）

対象は 4 種類 + 投与速度:

1. 上限量（1 回量／1 日量／体重あたりの極量／累積上限）
2. 1 日の使用回数の上限
3. 投与間隔（「6 時間以上あける」など）
4. 投与期間の上限
5. 安全性上重要な投与速度上限（「1 分間に 50mg を超えない」など）

書式:

- 1〜4 行。各行は `・` で始める。改行は `\n`。
- **表示テキストの記号は半角に統一**（`CLAUDE.md` の表に従う）。`、`→`, ` / `。`→`. ` / `（`→` (` / `）`→`)` / `：`→`: `
- 数値は必ず単位付き。例: `・1日4000mgまで`、`・1回200mg, 6時間以上あけて1日2回まで`
- 電子添文に上限規定が無い薬には書かない（「特に上限なし」という項目を作らない）。
- 電子添文外だが臨床上必須の上限（局所麻酔薬の極量など）は行末に ` (添文外)` と明記。
- **不確かな数値は書かない。網羅性より正確性を優先**（誤った上限は臨床上危険）。

### 完了時の検証

```bash
dart analyze lib test
flutter test
flutter build web --no-tree-shake-icons --base-href /masui_ref/
```

---

## 3. ユーザーからの回答待ち事項（着手しないこと）

1. **抗菌薬マトリクスの `▲` と `△` の統合可否**
   凡例では `▲`＝一部・条件付き、`△`＝限定的。だが実データ上 `△` は 4 個だけ
   （セフェピムの MSSA、ペニシリン G の腸球菌、メロペネムの Strep・MSSA）で、
   いずれも「活性はあるが第一選択にしない」の意。`▲`（23 個）との境界は原典由来で厳密でなく、表示色も同一。
   → 「`△` を `▲` に統合して 3 段階（○ / ▲ / ✕）にするか、色を分けるか」を提示済み・**未回答**。

2. **左列の略号への自動縮小適用**
   縦帯ラベルには自動縮小（`_bandFontSize`）を入れたが、左列の略号（`SM または SMX/TMP`、`PIPC/TAZ` 等）は未適用。
   テストフォント条件では省略が出る（実機では大半収まる見込み、詳細シートには全文あり）。
   → 「略号にも同じ処理を掛けるなら先に言ってください」と提示済み・**未回答**。

---

## 4. 直近の作業履歴（コンテキスト）

新しい順。

| コミット | 内容 |
|---|---|
| `30ac4ae` | 入室前チェックリスト: サクションチューブの注釈に太さ（気管内用 12Fr / 口腔内用 16Fr）を追加 |
| `b92dad7` | 抗菌薬・向精神薬マトリクスの縦帯（上位分類）を省略せず全て収める。帯幅 16→22px、最大 3 行、7.5pt→4.5pt の自動縮小 |
| （その前） | マトリクスのマーク刷新（抗菌薬 `●`→`○`、向精神薬 `●`→`◎`）、セル注記の詳細シートへの拾い出し、フォント -2pt、カバー／臓器移行性のセル幅統一 |
| （その前） | NSAID 追記: ロピオンに IBD 禁忌、ジクロフェナク（ボルタレン）・セレコキシブ（セレコックス）を新規追加 |
| （その前） | 入室前準備チェックリストの全面改訂（18→16 項目）、個別事例ゾーン（9 事例）新設、タブ順を 機能→薬剤→解説→緊急対応 に変更 |

補足:

- **国内 NSAID で潰瘍性大腸炎を「禁忌」とするものは無い**ことを一次情報（KEGG 収載の電子添文）で確認済み。
  ロピオンの記載は「炎症性腸疾患（UC / クローン病）」を **9.1 慎重投与相当**として入れてある。禁忌ではない。
- マトリクス系の生成スクリプトの入力 JSON（`.claude/book3_research/*.json`）もマーク置換済み。
  再生成しても `●` に巻き戻らないことを複製環境で実証済み。
- アプリ URL の QR コード（JPG）は MacBook の `~/Desktop/masui_ref_QR.jpg` にある。**リポジトリには入っていない**ので、必要なら作り直す（`segno` + Pillow、誤り訂正 H）。

---

## 5. 運用ルール（`CLAUDE.md` の再掲・特に守るもの）

- **デプロイはユーザーが明示的に言ったときだけ**（「dep」「deploy」「cctd」）。勝手に公開しない。
  - `cctd` = codex レビュー → `NO_FIX_NEEDED` ならデプロイ、というユーザー定義のショートカット。
- 自動デプロイは OFF（`deploy.yml` は `workflow_dispatch` のみ）。push してもデプロイは走らない。
  デプロイは `gh workflow run deploy.yml` → `gh run watch <id> --exit-status`。
- **push / deploy の前に必ず `git fetch` して `origin/main` との差分を確認する。**
  別セッションや codex が同じリポジトリを編集していることがある。必要なら `git pull --rebase`。
- 表示テキストの記号は半角統一（`CLAUDE.md` の表）。
- Bash のカレントが別プロジェクト（栄養計算機）にリセットされることがある。**必ず絶対パスで実行する。**

---

## 6. 引き継ぎ後の MacBook 側

このファイルを push した時点で MacBook 側の作業は止める。
以降の編集は mac mini 側で行い、MacBook で再開するときは先に `git pull --rebase` すること。
